// The opencode / opencode-go upsell flow was removed: this app only supports
// third-party providers, so there is nothing to upsell. Kept as a no-op because
// pages/session.tsx still calls it.
export function useUsageExceededDialogs() {}
