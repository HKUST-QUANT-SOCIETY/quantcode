# QuantumCode：量化投研 Agent 平台设计文档

## 1. 项目定位

### 背景
- **团队**：港科大量化初创 + 私募，5 人 agent 组服务于更大的量化团队
- **组织结构**：基本面组、因子组、模型组、风控组、策略组
- **覆盖标的**：期权、期货、高频、Crypto

### 核心叙事
> **「我们用 agent 把港科大的小团队，变成了 30 人投研团队的产能，所以我们能做到大机构因为人力成本不划算、单一精品店又没有广度去做的事——全标的、低相关、高频迭代的策略工厂。」**

**真正的壁垒不是「我们用了 AI」，而是：**
- 把投研流程编译成确定性的、机器可验收的 pipeline
- AI 在我们这里是可靠生产力，在别人那里只是 demo
- 跨组协商被替换为 schema 校验 + 自动化门禁

---

## 2. 核心方法论

### 2.1 任务拆分 + 确定性契约
- **问题**：长任务（10+ 小时）中 context compact 会丢失信息
- **解法**：
  1. 把大任务拆成无状态子任务
  2. 子任务间用 **确定性 JSON schema** 通信（不依赖自然语言）
  3. 状态外化到文件/JSON，支持 checkpoint 和重放
  4. 每个子任务 idempotent（可重启、可重放、结果一致）

### 2.2 100% 程序化验收
量化团队的天然优势：验收标准本就可量化
- **因子**：IC、IR、换手、衰减
- **策略**：夏普、回撤、容量
- **PR**：测试通过率、风控门禁

**验收标准 = `assert` 语句，不是人工检查。**

### 2.3 把人从 loop 中拿出来
- 其他组不需要学 prompt engineering
- 他们的接口是 YAML / 表单式 spec：「我要做什么、输入是什么、目标是什么」
- 提交进 pipeline，后面所有 agent 编排由 agent 组包掉
- 他们看到的只有：任务进队列 → 程序化验收报告 → 可复现 artifact

---

## 3. 架构设计

### 3.1 两层架构

| 层 | 内容 | 策略 | 原因 |
|---|---|---|---|
| **载体层** | IDE、桌面端、多用户界面、权限管理 | **不自建**，用现成 agent CLI/IDE（Claude Code / Mimo Code） | 载体层是 commodity，自建会被上游变更和合并冲突拖死 |
| **流程层** | JSON 契约、子任务编排、程序化验收、各组 skills/pipelines | **全力自建**，这是 IP | 这是真壁垒：港科大量化流程被编译成机器可执行 pipeline |

**实施方式**：不 fork Mimo Code 源码，基于扩展机制做加法
- Mimo Code 作为依赖（`import` 一个库）
- 定制全部放在 `skills/`、`pipelines/`、`schemas/` 等目录
- 上游更新 = 升级版本号，零合并冲突

### 3.2 「千人千面」的正确实现
不是做不同的 UI，而是：
- **相同的载体**（统一的 agent 界面）
- **不同的插件包**（每个组挂载不同的 skills + JSON 契约 + 验收标准）

示例：
- 因子组 → 挂载因子评估 pipeline
- 风控组 → 挂载回撤监控 pipeline
- 基建组 → 挂载拉数 pipeline

---

## 4. 核心 Pipelines

### 4.1 第一优先级：PR → 自动风控 JSON → CI Gate

**现状痛点**：
- 模型组提 PR 后，需要与风控组协商沟通
- 人工判断风险，流程慢、标准不一致

**agent 方案**：
1. 模型组提交 PR 时，CI 自动触发 agent
2. Agent 分析策略代码，自动计算风控画像并填充标准 JSON：
   ```json
   {
     "max_drawdown": 0.15,
     "position_limit": 0.3,
     "correlation_with_existing": 0.45,
     "capacity_estimate_usd": 50000000,
     "tail_risk_var_99": 0.08
   }
   ```
3. CI 拿这个 JSON 跑风控组预设的程序化阈值
4. 不满足 → 自动打回并附原因；满足 → 进入下一环节

**价值**：
- 人与人的协商 → 机器与机器的 schema 校验
- 风控标准被编码进 CI，24 小时自动执行、永不遗忘

### 4.2 基本面组：研究 → PDF

**需求**：
- 高效数据库设计（RAG）
- 快速产出专业级 PDF 研报（中金华泰水准）

**实现方案**：

#### RAG 层
- **量化 RAG 核心差异**：point-in-time 正确性
  - 研报、公告必须按「当时能看到的版本」检索
  - 不能让 agent 读到未来信息（lookahead bias）
  - Schema 中强制时间戳约束

- **数据层**：
  - 依赖基建组的数据基础设施
  - Agent 组加一层「研究友好访问层」：
    - 研报、公告、纪要切片向量化
    - 与结构化财务数据 join
    - 支持时点查询

#### PDF 生成
- **技术栈**：Typst + 自定义模板
  - 扒一份中金研报，拆解版式（封面、章节、图表、脚注、免责）
  - 锁定企业配色 + 字体规范
  - 图表统一用 matplotlib / plotly

- **工作流**：
  1. Agent 从 RAG 检索相关研报、财报
  2. 按模板生成结构化内容（markdown / JSON）
  3. 填充 Typst 模板
  4. 渲染 PDF

- **验收标准**：研究员愿意发出去 = 通过

### 4.3 因子评估 Pipeline

（待后续迭代，框架同上）

---

## 5. 实施路径：5 天冲刺

### 5.0 排期前提（为什么是 5 天）

- **5 人 agent 组，每人 24h 挂 Claude Code**：每个人不是「写代码的人」，而是「一条 track 的 agent 编排者」。人定义 schema 与验收 assert，agent 跑实现，人只在卡点和验收处介入。
- **并行而非串行**：5 条 track 用 §2.1 的确定性 JSON 契约解耦，track 之间只通过 schema 通信，不靠口头协商，因此可以真正并行而不互相阻塞。
- **产能预期**：传统 3 周（Phase 1 + 横向扩展）的范围，在这套编排下压到 5 天完成核心闭环 + 三条 pipeline 接入。

### 5.1 Track / 人员分工

| Track | Owner | 负责范围 |
|---|---|---|
| **T0 地基** | P1（Lead） | JSON Schema 框架、子任务调度器、checkpoint/重放、状态持久化 |
| **T1 风控门禁** | P2 | PR → 风控 JSON agent、CI gate、程序化阈值校验 |
| **T2 RAG** | P3 | point-in-time RAG 层、研究友好访问层、时点查询 |
| **T3 研报 PDF** | P4 | Typst 模板逆向、研报结构化生成、渲染 |
| **T4 因子 + 载体集成** | P5 | 因子评估 pipeline、验收 runner、Mimo Code 依赖集成、各组 YAML spec 接口 |

> **关键依赖**：T0 是所有 track 的上游。Day 1 上午 P1 必须先冻结 schema 接口与调度器 API，其余 track 才能 mock 着并行开工。一旦 schema 冻结，T1–T4 互不阻塞。

### 5.2 逐日时间线

#### Day 1 — 地基冻结 + 各 track 脚手架

**全员目标**：选型敲定，schema 与调度器 API 冻结，5 条 track 都能 mock 跑通空流程。

- **P1（T0）**：确定 Python vs TypeScript（默认 Python，量化生态成熟）；产出 v1 三套 schema（风控画像 / 子任务 I/O / 验收标准）；调度器骨架（任务拆分 + 状态持久化 stub）。
- **P2（T1）**：向模型组要一个真实 PR 作为测试样本；搭风控 agent 脚手架，先用 mock JSON 跑通「读 PR → 输出风控 JSON」。
- **P3（T2）**：选向量库（Chroma 起步，可换）；定义带时间戳约束的检索 schema；切片 + 向量化一小批研报作为样本集。
- **P4（T3）**：扒一份中金研报，拆版式（封面/章节/图表/脚注/免责）；搭 Typst 模板骨架 + 企业配色字体。
- **P5（T4）**：把 Mimo Code 作为依赖 `import` 进来，验证扩展机制；搭 `skills/ pipelines/ schemas/` 目录结构；验收 runner 脚手架。

**Day 1 验收**：schema 冻结并签入；调度器能跑一个 hello-world 子任务并 checkpoint；每条 track 都有可运行的空骨架。

#### Day 2 — 各 track 核心实现

**全员目标**：每条 track 的主逻辑跑通（用真实数据但允许局部 mock）。

- **P1**：实现 checkpoint/重放 + idempotent 重启；调度器支持子任务依赖编排；给 T1–T4 提供稳定调度 API。
- **P2**：风控 agent 真正分析策略代码，自动算出 max_drawdown / position_limit / correlation / capacity / VaR 并填 JSON；CI gate 读 JSON 跑风控组预设阈值。
- **P3**：研究友好访问层落地——研报切片与结构化财务数据 join；时点查询逻辑（过滤未来版本，防 lookahead）。
- **P4**：RAG 检索结果 → 结构化内容（markdown/JSON）→ 填 Typst 模板 → 渲染出第一版 PDF。
- **P5**：因子评估 pipeline v1（IC / IR / 换手 / 衰减自动计算）；验收 runner 能按 schema 自动校验并出报告。

**Day 2 验收**：T1 能对真实 PR 产出真实风控 JSON；T2 时点查询无 lookahead；T4 验收 runner 能跑出 pass/fail 报告。

#### Day 3 — 第一条 pipeline 端到端打通（风控门禁）

**全员目标**：锁定 §4.1 主线，PR → 自动风控 JSON → CI gate → 验收报告全程跑通。

- **P1 + P2 + P5 主攻**：把 T0 调度器、T1 风控 agent、T4 验收 runner 串成一条 CI pipeline；模型组真实 PR 进队列 → agent 生成风控 JSON → CI 执行程序化门禁 → 不满足自动打回并附原因，满足则放行 → 产出可读验收报告。
- **P3 + P4 并行推进**：RAG + PDF 继续打磨，准备 Day 4 接入；P4 让一位研究员试读 PDF 收集反馈。

**Day 3 验收（核心里程碑）**：一个模型组的真实 PR 走完全流程，**风控组认可输出结果**。这是项目的第一个可对外展示的闭环。

#### Day 4 — 横向扩展：接入 RAG + PDF + 因子

**全员目标**：把第二、第三条 pipeline 接进统一调度与验收框架。

- **P3 + P4**：RAG + PDF pipeline 正式接入调度器，走「任务进队列 → 程序化/半程序化验收 → 可复现 artifact」的统一路径；验收标准：研究员愿意发出去 = 通过。
- **P5**：因子评估 pipeline 接入；打磨各组 YAML / 表单式 spec 接口（其他组只填「做什么、输入、目标」，不碰 prompt）。
- **P1**：抽象出 pipeline 模板库雏形，让新 pipeline 复用调度 + 验收骨架。
- **P2**：风控门禁固化为 CI 常驻任务，24h 自动执行；补边界 case 与打回原因模板。

**Day 4 验收**：≥3 条 pipeline（风控 / RAG-PDF / 因子）跑在同一框架上；至少 1 个非 agent 组同事通过 YAML spec 成功提交一个任务。

#### Day 5 — 集成加固 + Demo + 文档

**全员目标**：把 5 天产物收敛成可演示、可交接、可复现的状态。

- **全员**：跨 track 集成测试，跑通混合任务队列；补错误处理与降级（agent 失败时的重试/打回路径）；清理临时产物。
- **P1**：监控与日志基线（任务状态、失败率、耗时）。
- **P4 + P3**：产出投资人 demo 素材——agent 生成的中金水准研报 PDF + RAG 时点检索演示。
- **P2**：产出 CI 风控门禁的运行 log 作为「24h 自动执行」证据。
- **P5 + P1**：写最小交接文档 + pipeline 模板库使用说明，让新 track 能在 1 天内上线。

**Day 5 验收**：三条 pipeline 集成可演示；一份投资人级 demo（研报 PDF + 自动化风控 CI log + 因子迭代数据）成型；文档可交接。

### 5.3 风险缓冲

- **schema 冻结延迟**：T0 是关键路径，若 Day 1 未冻结，全员阻塞。P1 优先级最高，必要时 Day 1 全员协助定 schema。
- **半程序化验收**（如研报「愿意发出去」）：Day 1 就明确哪些 track 可 100% 程序化、哪些走人工 gate，不强求全覆盖（见 §7.2）。
- **真实 PR / 数据获取**：Day 1 必须拿到模型组真实 PR 与研报样本，否则后续 track 只能 mock，验收失真。

---

## 6. 技术栈

### 核心依赖
- **Agent 载体**：Mimo Code（作为依赖，不 fork）
- **LLM**：Claude Opus 4.8（通过 Mimo Code 接入）

### 自建流程层
- **Schema 定义**：JSON Schema + YAML
- **任务编排**：Python / TypeScript（待定）
- **状态管理**：文件系统 + JSON checkpoint
- **RAG**：
  - 向量数据库：Chroma / Pinecone / Weaviate（待选型）
  - 时点查询：自建逻辑层
- **PDF 生成**：Typst + Python 模板引擎
- **CI 集成**：GitHub Actions / GitLab CI

---

## 7. 关键风险和应对

### 7.1 RAG 的 Point-in-Time 正确性
- **风险**：检索到未来信息导致 lookahead bias
- **应对**：Schema 强制时间戳，检索时过滤版本

### 7.2 验收标准定义不清
- **风险**：某些组（如基本面研究）产出难以 100% 程序化
- **应对**：Phase 1 先做能程序化的（风控门禁），不强求全组覆盖

### 7.3 维护成本
- **风险**：流程层代码也需要维护
- **应对**：
  - 不 fork，避免上游合并冲突
  - 明确 agent 组专职维护
  - 建立 pipeline 模板库，复用 > 重写

---

## 8. 成功指标

### Day 3 目标（核心里程碑）
- [ ] 一条 PR → 风控 JSON → CI gate pipeline 跑通
- [ ] 一个模型组同事用上并认可

### Day 5 目标（冲刺收尾）
- [ ] ≥3 条 pipeline（风控 / RAG-PDF / 因子）跑在同一调度 + 验收框架上
- [ ] 至少 1 个非 agent 组同事通过 YAML spec 成功提交任务
- [ ] 一份投资人级 demo 成型：中金水准研报 PDF + 24h 自动化风控 CI log + 因子迭代数据
- [ ] pipeline 模板库雏形 + 可交接文档（新 track < 1 天上线）

### 后续目标（5 天之后）
- [ ] 所有组接入并日常使用
- [ ] 平均每个组提效 > 30%（用节省的人工小时数衡量）
- [ ] 监控、降级、性能优化等生产化加固

---

## 9. 对外叙事（给投资人）

**一句话**：
> 我们用 agent 把投研流程编译成了确定性的、机器可验收的 pipeline，所以我们能用 5 人的成本做 30 人的产出，覆盖大机构不划算、单一精品店没广度的全标的策略空间。

**核心证据**：
1. **业绩曲线**（首要，叙事服务于此）
2. **实际案例**：
   - 5 分钟生成中金水准研报（展示 PDF）
   - 风控门禁 24 小时自动执行（展示 CI log）
   - 因子迭代速度是传统团队的 3 倍（展示数据）

**壁垒解释**：
- 不是「我们会用 AI」（人人会说）
- 而是「我们把量化流程的隐性知识编译成了显性的、可执行的 schema」（别人没耐心做）
- Agent 在我们这里是可信赖的生产线，在别人那里只是 demo

---

## 10. 下一步行动（启动 Day 1 前）

**Day 1 开工前必须就绪**：

1. **人员锁定**：5 名 agent 组成员对应 T0–T4 的 owner，每人确认 24h Claude Code 接入可用。
2. **样本就位**：从模型组拿到一个真实 PR；从基本面组拿到一批研报样本（含中金 PDF 一份用于版式逆向）；从基建组确认数据访问凭证。
3. **技术选型预判**：流程层语言默认 Python（量化生态对齐），向量库默认 Chroma；Day 1 上午 P1 复核后冻结。
4. **沟通机制**：约定每日 15 分钟全员 standup（早 + 晚各一次），其余时间通过 schema + checkpoint 异步协作，避免开会拖慢 24h 产能。

**Day 1 一开工**：见 §5.2 Day 1 任务清单，全员同时启动。

---

**项目代号**：QuantumCode  
**Owner**：Agent 组  
**文档版本**：v1.0  
**最后更新**：2026-06-24
