# model 组 tool 白名单 — Day 4 俞高磊
# ★ run_agent 是 meta tool，不在此 allowlist；AgentRunner 内部 agent 只看到下方单步 tool + 共享工具
allowlist:
  # 单步 tool（AgentRunner 内部使用）
  - read_pr
  - extract_metadata
  - generate_model_spec
  - write_blackboard
  - trigger_risk_flow
  # 共享工具
  - search_memory
  - read_file
  - write_file
  - bash
