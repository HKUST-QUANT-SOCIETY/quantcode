# portfolio 组配置（configs/portfolio.yaml）— 三工具默认阈值/成本的单源。
# Schema 默认（schemas/portfolio.py）与本文件保持一致；运行覆盖优先级：
# tool 入参 > 本文件（tools/portfolio/rebalance.py 读 portfolio key）> 代码默认。
max_single_weight: 0.10
commission: 0.0003
stamp_tax: 0.0005
rebalance_min_turnover: 0.05
max_gross_exposure: 1.0
max_turnover_gate: 0.50
max_drawdown_proxy: 0.20