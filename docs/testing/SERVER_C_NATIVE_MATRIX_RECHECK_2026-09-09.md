# Server C cross-member matrix recheck

Final result after the gateway transport fix: **112/112 identical-target checks passed, with eight verified logouts and no request retries or model calls**. The final raw report is `SERVER_C_NATIVE_ALL8_CROSS_ONLY_SOURCE11_VERIFIED_2026-09-09.json`; an independent coverage, request-target and environment check is recorded in `SERVER_C_NATIVE_MATRIX_FINAL_VERIFICATION_2026-09-09.json`. The unsuccessful earlier attempts remain below for traceability.

The earlier `SERVER_C_NATIVE_ALL8_CROSS_ONLY_RELEASE9_2026-09-09.json` is preserved as historical evidence. Its 112 denial responses do not establish a complete isolation pass: owner artifact reads used a refreshed source revision while cross-member reads used the old lifecycle revision, and its client cleanup did not log out the eight identities.

`tests/e2e/verify_existing_native_matrix.py` now uses the same relative URL, source revision, artifact ID and offset for owner and reader requests. Each task/artifact check requires owner-before HTTP 200, cross-member denial, reader-own HTTP 200, and owner-after HTTP 200 on the same target. Artifact reads also verify the original bytes against the lifecycle SHA-256. Every request is recorded and no request is retried. All eight identities are logged out even after failure, followed by checks that their original tasks and workspace discovery are inaccessible.

The first strict recheck is preserved in `SERVER_C_NATIVE_ALL8_CROSS_ONLY_RELEASE9_VERIFIED_2026-09-09.json`. Its result is **failed**, not a 112-check pass. The fundamental-to-factor task check passed. For factor artifact revision 164, owner-before returned 200, cross-member returned 400, reader-own returned 200, and owner-after returned 400 for the identical artifact URL. Because the owner's resource was no longer readable at the final check, the artifact denial cannot be attributed exclusively to cross-member access control.

The recheck completed all eight logouts and confirmed HTTP 400 for both original-task access and workspace discovery afterward. It reused the eight real completed tasks and made no model requests. The earlier successful member lifecycles remain valid evidence for those workflows; the cross-member matrix remains incomplete pending investigation of the recorded owner-read failure.

A second independent strict run also failed at the first pair's artifact owner-positive read; its evidence is `SERVER_C_NATIVE_ALL8_CROSS_ONLY_RELEASE9_VERIFIED_RERUN_2026-09-09.json`. This run also completed eight verified logouts. Neither run retries a failed HTTP request.

Read-only inspection confirmed factor's published source revision remained 164, while ports 7702 and 7902 have distinct actual process XDG paths, SQLite file inodes and publisher source IDs (`SERVER_C_FACTOR_HOST_ISOLATION_2026-09-09.json`). With eight authenticated members, a direct transport comparison recorded 12/12 successful Python HTTPX gateway reads but four `ECONNRESET` errors in 12 Bun reads of the identical artifact (`SERVER_C_ARTIFACT_TRANSPORT_2026-09-09.json`). A separate request-side comparison reproduced five resets in 12 default Bun requests and 12/12 successful requests with explicit `Connection: close` (`SERVER_C_ARTIFACT_CONNECTION_AB_2026-09-09.json`). Every successful response retained revision 164 and all diagnostic logins were revoked afterward.

The gateway used the Python handler's default HTTP/1.0 close behavior without an explicit response `Connection: close` header. The observations supported a connection-reuse failure between Bun and that gateway, rather than a changed artifact version.

The fix explicitly sends `Connection: close` and sets `close_connection = True`, preserving HTTP/1.0, JSON responses and authentication. QA gateway 5097 alone was moved to immutable source11, derived from its original source01 with only `gateway.py` and that archive's gateway source-review hash changed. Its database inode, roster and registry remained unchanged; formal gateway 4097 retained its original PID, roster/registry hashes and runtime target.

With the server fix and the original CLI8 default fetch behavior, all 12 Bun reads and all 12 native API reads succeeded without an extra request header; the Python and explicit-close control groups also passed (`SERVER_C_ARTIFACT_TRANSPORT_SOURCE11_2026-09-09.json`). The subsequent strict matrix passed all 56 directed member pairs for both task and artifact resources: 112 denied cross-member reads, 224 successful owner-before/after reads, 112 successful reader-own reads, exact request-target equality, and eight verified session revocations. No individual request was retried. The eight completed native tasks and original artifact hashes were reused.

The final storage observation reports 1,890,365,440 available bytes (approximately 1.76 GiB) on the Server C root filesystem. Other users' files were not removed. Production capacity remains an operational follow-up; no additional tests were run after the final strict matrix and its evidence verification.
