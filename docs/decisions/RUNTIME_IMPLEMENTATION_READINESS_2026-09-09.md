# 内化源码交付与验证边界

日期：2026-09-09。最新状态：**平台基础验收通过；全员量化业务正式上线未通过**。

后续验收结果见 [2026-09-09 QA 执行报告](../testing/QUANTCODE_QA_REPORT_2026-09-09.md)：已完成当前源码 UI、Server C 八成员全流程、112 项同版本隔离检查及新 Electron 包的真实任务。本文下方保留最初交付时的判断和当时阻断；测试结果、历史用例缺口与业务组件接入条件以该报告为准。

本次只按 `QUANTCODE_RUNTIME_INTERNALIZATION_2026-09-08.md` 的七项标准和 M0～M4 退出条件核对当前工作树。没有将更早的双循环审计清单当作当前实现，也没有把其他产品路线的后续规划扩大成新的迁移内核。当前工作树包含大量既有未提交修改，全部保留；本记录不代表提交、发布或正式部署。

## 七项标准的当前依据

| 标准 | 当前源码/已有 UI 依据 | 尚缺的证明 |
|---|---|---|
| 一份源码与发布流程 | `frontend/packages/desktop/scripts/prebuild.ts` → `packages/opencode/script/build-node.ts` → desktop utilityProcess；本仓发布配置；`scripts/install_native_host.py` 复用 compiled CLI `serve` | 干净依赖重放、构建、安装包、升级和真实宿主安装 |
| 一套产品入口 | 原文进入 `QuantCodeHome`/`tabs.newDraft`/既有 `session.promptAsync`；身份、设置、目录、项目动作、GitGraph/Admin 已适配；最近修复草稿页、项目编辑与侧栏动作 | 最近修复的全部桌面交互、错误/取消/重登、原生任务树与恢复控件 |
| 一套模型配置 | `Auth`/`Config`/`Provider` 为唯一模型来源；`config-policy.ts` 与 Auth metadata 绑定 API URL；主子任务及辅助请求经过原预算；显式模型导入保留目标绑定 | 真实 URL/API Key 单次设置驱动任务，撤销/断流/重定向/部分保存与用量行为 |
| 一套任务事实源 | 原 Session/Message/Part/Event；`task-index.ts` 派生授权摘要，`task-publisher.ts` 投递；原 Runner/BackgroundJob 负责执行与取消 | 真实事件、取消、断线重开、跨成员汇总与组织投影一致性 |
| 组织约束进入执行链 | `identity.ts`/`workspace.ts`/`write-policy.ts`/Tool wrapper/MCP/TaskTool，目录与文件 helper、OS sandbox、方案、复用、预算、Gate 和回执 | M1 六项退出条件的正反场景及真实进程证明；不能仅凭拒绝代码存在通过 |
| 可持续维护 | 已有包边界、生成器、回归源码、许可证及发布流程；[集中测试执行计划](../testing/FINAL_TEST_EXECUTION_PLAN_2026-09-09.md) 对照真实 package scripts | 完整用例、类型、构建、依赖升级和打包证明 |
| 有边界的兼容 | 原生新任务与 legacy 读取/恢复分离；旧 owner/检查点/来源/图参数核对；原许可证与兼容包名保留 | 原始历史完整性、不重复副作用、真实退役、迁移与升级验收 |

“可进入 UI 核验”只表示本次限定源码核对未找到必须先补代码的新增明确阻断，不表示不存在缺陷，也不替代编译、测试或行为证据。

## 各阶段未完成的退出证明

- **M0**：现状、迁移映射和保留边界已有交付；以主决策与当前源码为准，历史草稿的完成声明不采用。
- **M1**：未登录、组越权、生产部署拒绝、L2/L3 冻结、缺能力征询及原生工具旁路均有执行落点；运行验收未完成。
- **M2**：仍需真实完成“查询能力 → 读授权文件 → 调用一个已接入组件 → 记录结果”，证明只有一份模型配置且主子任务用量归属正确。
- **M3**：仍需断线重开继续、真实进程停止、跨成员 Admin 汇总及审批、原版本 legacy 恢复不重复副作用。最近的取消时序和审批结果核对只有源码/回归材料。
- **M4**：入口守卫、产品适配、安装准备和兼容材料已写；真实切换与旧循环退役必须在 M1～M3 通过后进行，不能提前删除历史或启用正式迁移。

真实组织 gateway 之前对新版任务索引返回 404，是当前部署不足的证据；隔离 fixture 不能将它变成 PASS。个人原生宿主、工具发布目录、工作区、共享组织服务和真实模型/组件还需配齐并验证。

## 当前阻断与恢复顺序

桌面 CUA 工具的自动审批检查已在连续三轮目标处理中超时。最近一次先重置工具 JavaScript 会话，再只读获取原目录核验标签，仍在约 90 秒后超时；未给出安全拒绝理由。重置仅清除工具绑定，没有关闭或重启任何应用。监听检查确认隔离预览 `5844/5796`、组织 fixture `5344/5296` 和真实账号预览 `5644/5596` 仍在运行；服务在线不能替代 UI 核验。

按用户明确顺序，当前不能跳过 UI 而开始用例测试。没有继续扩张代码审计、修改需求或把测试计划当作执行结果。等待 UI 工具的自动审批服务恢复后依次进行：

1. 使用 CUA 当前标签清单重新绑定页面，先确认真实 URL/宿主；若原标签已关闭而服务仍在线，可重新打开该 URL，不重启服务。
2. 完成 `5844` 的授权子目录最终选择、取消保留草稿、宿主切换与失效状态；之前只证明列表可见和选择按钮可用。
3. 以当前源码核验模型保存、项目编辑、侧栏授权目录、完整任务树/停止、审批提交结果核对、预算/回执/旧恢复页面。需要新后端源码时另建隔离宿主，不复用旧进程冒充新版本。
4. 更新 [桌面 UI 核验记录](../testing/DESKTOP_UI_REVIEW_2026-09-08.md)，只有真实覆盖的 UI 项可记通过；动态场景不能用静态组织摘要证明。
5. UI 通过后按[集中测试执行计划](../testing/FINAL_TEST_EXECUTION_PLAN_2026-09-09.md)收集当前全集，与原 **1300 个 nodeid** 逐项对照，再跑全部 Python/Bun/桌面/E2E/类型/构建/安装及最终场景。缺失、失败、跳过和外部阻断不能算全部通过。
6. 完成真实模型/组件、组织服务及安装部署验证，再逐条审计七项标准和 M1～M4 退出条件。全部证明齐备前保持目标未完成。

本轮未执行测试收集、用例、类型检查、产品构建、安装或正式部署；没有修改已有凭据、roster、历史或进程。
